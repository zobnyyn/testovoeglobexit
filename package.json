{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "tsc && node build/server.js",
    "docker:build": "docker build -t fastify-test-server server",
    "docker:up": "docker run -d -p 3001:3001 -e PORT=3001 --name fastify-container fastify-test-server",
    "docker:stop": "docker stop fastify-container"
  },
  "author": "globexit",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^11.0.1",
    "@fastify/static": "^8.3.0",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
